<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Keep It Note" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%# <meta name="apple-mobile-web-app-capable" content="yes"> %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# <link rel="manifest" href="/manifest.json"> %>
    <link rel="icon" href="/notes.png" type="image/png">
    <link rel="icon" href="/notes.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/notes.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "https://use.fontawesome.com/releases/v6.4.2/css/all.css", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet">
  </head>
  <body>
    <main class="">
      <%= render 'shared/flash' %>
      <% if user_signed_in? %>
        <%= render 'shared/header' %>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
